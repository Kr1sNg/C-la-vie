#include "vcb.h"

bool ft_check_token(char *av)
{
    int i = 0;
    while (av[i + 1])
    {
        if ()
    }
}



int main(int ac, char **av)
{
    if (ac != 2)
        exit(1);
    ft_parsing(av[1]);

}